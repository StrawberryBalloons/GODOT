using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlantSeed : MonoBehaviour
{
    public Seed seed;
    //the purpose of this script is to attach to a seed in a plant prefab, once the seed has fully grown then it is thrown
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
